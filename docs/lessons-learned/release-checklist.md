# 稳定版本检查表

## Git

- [ ] `git status` 中没有意外文件。
- [ ] `chrome_profile/`、缓存和凭证没有进入 Git。
- [ ] 当前修改有清晰提交。
- [ ] 已成功推送到 GitHub。

## 自动测试

- [ ] Python 编译检查通过。
- [ ] `python -m unittest discover -s tests -v` 通过。
- [ ] 新修复有对应回归测试，或记录为何无法自动测试。

## 启动

- [ ] 一键启动脚本可运行。
- [ ] Streamlit 页面可打开。
- [ ] 专用 Chrome 的调试端口 `9222` 可访问。
- [ ] Amazon 与 SellerSprite 登录状态正常。

## 类目

- [ ] 类目弹窗在 100% 缩放和笔记本视口中可完整使用。
- [ ] 搜索、展开、勾选、全选、清空、取消、确认均正常。
- [ ] 勾选父类会包含全部后代。
- [ ] 确认与取消按钮始终固定可见。
- [ ] 所选类目能够解析为正确的叶子入口。

## 采集

- [ ] 只打开当前计划中的入口。
- [ ] 每页按锁定策略等待和滚动。
- [ ] SellerSprite 字段完成后才读取。
- [ ] 第一页完成后点击真实下一页按钮。
- [ ] 页面采集后及时关闭，避免内存持续增加。
- [ ] 停止采集可在当前安全点真正停止。
- [ ] 进度显示当前类目、页码、累计原始产品和失败入口。

## 筛选与结果

- [ ] 清空输入框表示不限。
- [ ] 没有隐藏后台默认筛选值。
- [ ] 放宽筛选可从原始池恢复更多产品。
- [ ] 缺失字段不会被错误伪装成真实的零。
- [ ] 筛选报告能解释主要排除原因。

## 导出

- [ ] 仅导出勾选产品。
- [ ] 全选状态与实际勾选数量一致。
- [ ] 中文表头、列对齐和数据类型正确。
- [ ] 图片公式和 ASIN 超链接正确。

## UI

- [ ] 100% 浏览器缩放下无横向溢出。
- [ ] 工具栏和表头冻结时不互相覆盖。
- [ ] 无多余白条、重复边框或层级断裂。
- [ ] 按钮、输入框和标签在常用视口对齐。

